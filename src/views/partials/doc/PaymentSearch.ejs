<div class="row">
    <h2 class="mb-3 text-center">Advanced payment searches</h2>
    <section class="col-md-8">
        <article class="mb-5">
            <h2 class="mb-3 text-center">GET by id</h2>
            <div>
                <h4>URL</h4>
                <pre><%= urlBase.url %>/api/payment/id</pre>
            </div>
            <div>
                <h4>Header da solicitação</h4>
                <div>
<pre>
    header = {
        "method": "GET",
        "Authorization": {token},
        "id_payment": 10,
    }
</pre>
                    <div>
                        <p>id_payment: id do pagamento deve ser passado como parâmetro da requisição</p>
                    </div>
                </div>
            </div>
        </article>
    </section>
    
    <section class="col-md-8">
        <article class="mb-5">
            <h2 class="mb-3 text-center">GET by user</h2>
            <div>
                <h4>URL</h4>
                <pre><%= urlBase.url %>/api/payment/user</pre>
            </div>
            <div>
                <h4>Header da solicitação</h4>
                <div>
<pre>
    header = {
        "method": "POST",
        "Content-Type": 'multipart/form-data',
        "Authorization": {token}
        "id_user": 60,
    }
</pre>
                <div>
                    <p>id_user: deve ser passado como parâmetro da requisição o id do usuario</p>
                    <p>Essa pesquisa retornará todos Pagamentos do usuário independente do status</p>
                </div>
            </div>
        </article>
    </section>

</div>
